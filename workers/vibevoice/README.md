# vibevoice worker

Isolated experimental Microsoft VibeVoice streaming ASR worker. It remains disabled by default
until its very recent checkpoint passes the local stability and repetition benchmark.
