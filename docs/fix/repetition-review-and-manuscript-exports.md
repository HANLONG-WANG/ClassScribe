# 重复异常提示、导入卡片与稿件多选导出

2026-09-09

- 重复检测区分课堂主题词复现与大面积短语重复/连续循环。重复异常不直接作废；有正常候选时优先采用正常候选，全部异常时保留一个完整候选并标红待复核。
- 转录句段的红色标签可以点击进入检查面板，查看触发原因、重复片段和来源模型。标签依据持久化候选与采用来源生成，重新打开和对齐后仍保留。
- 历史恢复只针对已结束任务中旧规则生成的占位段，保护人工修改，保留审计记录，不重复运行模型。此次在完整备份后恢复了本地 11 段，第二次执行恢复数量为 0。
- 导入页移除了“查看转录队列”长条按钮，重新排布进度卡片的标题、汇总、每文件进度条、错误和重试操作。
- 导出配置仍位于顶部，下方以历史稿件列表提供复选框、跨页选择、全选本页和清空选择。每份稿件调用独立导出事务，生成独立文件；一份失败不影响其他稿件。没有正文的任务不可选。
- 窄屏导航补充可读名称；转录时间轴在音频未加载时使用录音元数据或句段范围，避免撑破布局。

验证涵盖质量门、单/多异常候选共识、历史恢复幂等与编辑保护、API/流水线、前端跨页多选与部分失败，以及 1440px 和 390px 的浏览器交互与布局。置信度展示未在本次改动中重新定义。
