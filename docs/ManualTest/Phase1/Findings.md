# Phase 1 发布前用户视角测试

## 缺陷索引（2026-09-13更新）

状态说明：以下均为已记录的现象；F-006仍需独立手动操作交叉验证。测试未完成，列表不是发布放行结论。

| ID | 影响 | 问题 |
| --- | --- | --- |
| F-001 | 低 | favicon请求404 |
| F-002 | 中 | 词条缺读音静默拒绝；超长词典名422不显示 |
| F-003 | 中 | 模型回滚409无反馈 |
| F-004 | 中 | 材料建议缺直接确认/编辑及手工alias等入口 |
| F-005 | 低 | 同文件重选不触发材料导入，跨词典亦复现 |
| F-006 | 高，待人工交叉验证 | 导入页有效WAV选择后列表为空 |
| F-007 | 中 | 离开导入页后配置草稿重置 |
| F-008 | 中 | 无历史撤销/重做409无提示 |
| F-009 | 中 | 多窗口版本冲突仅重试无法恢复 |
| F-010 | 中 | 用户版空句段显示与导出回退不一致 |
| F-011 | 中 | 低置信筛选隐藏其他时间轨 |
| F-012 | 中 | 编码/CSV字段输入错误500且无UI提示 |
| F-013 | 中 | CSV确认词条被材料持久层拒绝，整份500 |
| F-014 | 低 | 合法长名称挤压导入按钮 |
| F-015 | 中 | 大写.CSV误按TXT解析 |
| F-016 | 中 | 候选卡缺完整revision/解码等详情 |
| F-017 | 中 | 存储错误日志输出转录正文SQL参数 |
| F-018 | 中 | 导出缺粗时间标记 |
| F-019 | 高 | 保存失败草稿刷新丢失 |
| F-020 | 低 | 诊断包遗漏IBus/Portal状态 |
| F-021 | 低 | 主导航切页保留滚动位置 |
| F-022 | 中 | 模型需修复却同时宣称健康通过可用 |
| F-023 | 中 | 安装确认区缺明确模型身份与完整revision |
| F-024 | 中 | 完整工作台Playwright用例使用旧标题/按钮而失效 |
| F-025 | 高 | 架构规则检查发现consensus中禁止的长文本序列匹配 |
| F-027 | 中 | 历史回归清单引用已不存在的测试函数 |
| F-028 | 高 | 发布校验将final_architecture判真，但独立架构门失败 |
| F-029 | 中 | 应用错误API token后界面继续展示旧数据且无鉴权错误 |
| F-030 | 中 | 浏览过任务后刷新任意页面都会跳回旧任务详情 |
| F-031 | 高 | IBus验证脚本会chmod输出文件的既有父目录为0700 |
| F-032 | 低 | 390侧栏隐藏导航文字且无title提示，只剩抽象符号 |
| F-033 | 高 | 用户手册承诺的三层本地数据删除没有Web/API入口 |
| F-034 | 高 | WebUI材料导入缺PDF/PPTX/讲义/教材入口 |
| F-035 | 中 | 已完成导入进度永久保存在浏览器且无清除/后端校验 |

编号说明：F-026曾用于记录调度测试挂起，沙箱外复测全部通过后已撤销，保留空号避免改写历史引用。

## 状态与约束

- 状态：进行中，不能据此宣布全量测试完成。
- 开始日期：2026-09-12；代码：`c19261da2dac5954d1ce0fb926f39142ec5328c6`。
- 本轮开始时目标记录文件不存在，没有可继承的已完成测试证据。
- 不启动大模型、不执行真实语音转写；模型推理为用户明确排除项。
- 真实生产前端构建 + 真实 API；独立 XDG 根 `/tmp/classscribe-manual-phase1/{config,data,cache,state,runtime}`，不操作用户既有数据。
- 服务启动脚本 `/tmp/classscribe-manual-phase1/serve.py`，地址 `http://127.0.0.1:8765`；scheduler IPC 未启用，默认prewarm=false。初始无模型，后续仅加入已校验的4.65MB VAD隔离副本用于文件管理UI测试，未加载。
- 现有 `frontend/e2e/workbench.spec.ts` 使用 route 模拟 API，仅可作补充，不能替代真实 API 用户测试。
- GitNexus ClassScribe 索引提交与 HEAD 相同；已用 Serena 核对应用入口。

## 覆盖清单

所有项目均需进一步分解操作、正常/空/错误状态、持久化和 UI 验证。

| 页面/功能 | 状态 |
| --- | --- |
| 全局导航、品牌、刷新、键盘、窄屏与视觉 | 可自主范围已测；见NAV/KB/UI，含F-021/F-030/F-032 |
| 导入：单/多文件、类型、语言、策略、模型、词典、人数、输出、失败重试 | F-006阻断文件列表；接口/配置已测，人工交叉验证按用户要求跳过 |
| 任务队列：状态、筛选、选择、批量操作、恢复 | 可自主非推理范围已测；真实checkpoint执行排除 |
| 任务详情：进度、日志、暂停/继续/取消/重试 | 各终态与暂停/取消已测；会启动执行器的继续/重试跳过 |
| 转录稿列表：筛选、搜索、删除、打开 | 当前可见列表/分页/空正文/打开已测；产品无搜索/删除入口，删除缺口见F-033 |
| 转录详情：播放、时间轴、层、校对、证据、编辑、撤销重做、说话人 | 可自主非推理范围已测；重跑当前范围跳过 |
| 导出：格式/文本层/布局、生成、下载、内容 | 六格式/失败恢复/边界已测；浏览器落地按用户要求跳过 |
| 词典：创建、编辑、术语、材料导入与确认、边界 | 当前可见控件已测；功能缺口见F-004/F-034 |
| 模型：列表、筛选、安装披露/取消、不可用状态；真实推理排除 | 非推理文件管理/披露/健康WAV前置已测；删除/修复/最终确认跳过 |
| IBus：设置、状态、服务不可用、非推理交互；真实转写排除 | Web不可用/降级/窄屏已测；预热与桌面输入跳过 |
| 设置：保存、验证、持久化、诊断/下载、数据清理 | 现有控件与诊断已测；数据清理入口缺失见F-033 |
| 桌面入口/IBus 集成非推理交互 | 本机未安装，按用户要求跳过 |

## 测试记录

### ENV-001 — 构建与隔离服务
- `npm --prefix frontend run build`：通过，TypeScript 与 Vite 构建成功。
- pnpm 命令在当前 PATH 不存在，使用项目同一 build script 的 npm 入口执行。
- 沙箱内 loopback bind 失败；获得工具自动审查允许的 escalation 后服务启动成功。
- 此项仅证明测试环境可启动，不证明页面可用。

## 缺陷

已记录 F-001 至 F-006，详见各条复现及证据；F-006 为高影响导入入口问题。未测试不等于通过。

### UI-001 — 首页首次打开（部分）
- 全新 playwright-cli 浏览器显示导入页、8 个主导航项、语言/词典/人数/主模型/4 个处理模式/6 格式和文本层选项；无文件时入队按钮禁用。
- 无安装模型时明确显示必需与可选模型“尚未就绪”。
- 控制台发现 `/favicon.ico` 404（低影响资源缺失，F-001）。证据：`.playwright-cli/page-2026-09-12T06-44-10-727Z.yml` 与同目录 console 日志。
- playwright-cli 后台会话未能跨命令保持，改用 CUA Chrome 继续真实交互。
- Chrome 同源保留旧导入进度（2 个已入队文件），但隔离后端无这些记录；作为跨后端/浏览器持久状态待验证项，不宣称正常用户必现缺陷。

### GL-001 — 词典空状态及创建
- 点击词典：出现“创建第一个课程词典”。点击空名称的 ＋ 不创建，但没有可见验证消息。
- 输入 `Phase1 中文・日本語 English` 点击 ＋：创建成功并自动打开，列表 0 个词条/v1，显示导入、删除、词条表单。
- 待测：术语写入/重复/空白、刷新、材料导入、删除。

### F-001 — favicon 资源缺失（低）
- 首次打开首页，浏览器请求 `/favicon.ico` 返回 404；页面主内容正常。
- 预期：提供应用图标或显式配置 favicon，发布界面无缺失站点图标请求。

### GL-002 — 手工词条和刷新
- 新增 `量子力学` / `liàng zǐ lì xué` / 中文成功，表格显示 `zh · manual · 已确认`，权重 `1.00`。
- 刷新后页面回到导入页；重新点击词典后词条与读音仍存在，持久化通过。导入页课程词典下拉同步出现新词典。
- 提交纯空格标准写法没有增加词条，也没有可见提示。
- 同语言再次提交 `量子力学`，没有增加词条、输入保留，也没有可见错误或重复提示；正在核查服务响应。
- 2048×977 视口截图观察：词典两栏、表格、中文/日语/拉丁读音无明显遮挡截断。其他尺寸未测试。
- 导航刷新位置丢失为已观察行为（词典→刷新→导入），待跨页面核查和影响定级。

### GL-003 / F-002 — 缺少读音时静默拒绝提交（中）
- 输入标准写法 `量子力学`、中文，读音留空，点击“加入词典”：无请求、无提示、输入保留。DOM 确认两个 textbox 均 `required=false` 且 `validationMessage=''`。
- 补上读音 `duplicate` 后同一次提交成功：原词条读音变为 duplicate，词条总数保持 1。此前“重复无反馈”实际由读音为空触发，不能误记成服务错误。
- 预期：若读音必填，应明确标注并显示验证消息；若可选，应允许留空。目前用户无法判断为何按钮无效。
- 重复 canonical+language 为覆盖更新行为，UI 按钮仍称“加入词典”，无覆盖提示。记录为可用性待评估项。

### QU-001 — 空队列、全局暂停恢复、历史入口
- 真实空数据库显示等待 0 节、队列已清空；“暂停整个队列”切换为“恢复整个队列”并显示暂停说明。
- 刷新并重新进入任务队列仍保持暂停，持久化通过。点击恢复后重新显示“队列自动运行”。此时无任务，无模型运行。
- “查看已完成稿件”正确进入历史转录稿；0 份时显示导入提示，上一页/下一页禁用，页码 1。
- 本项不覆盖有任务调度、单课/批量操作和分页非空边界。

### EX-001 — 导出空状态
- 0 份稿件时生成、全选、清空、前后翻页按钮均禁用；页面显示先导入音频提示。
- 格式/文本版本/组织方式可操作（已选择 SRT、用户版、段落）；无稿件不能生成。
- 真实导出生成、下载及六格式内容尚未测试。

### ST-001 — 设置与诊断首次显示
- 设置页正确加载，默认派生缓存 30 天、IBus 原始音频未勾选。
- 诊断返回 Fedora/GNOME/Wayland、GPU、FFmpeg/PipeWire/IBus/Portal 状态；workers/models 空，dictationd 不可用，明确展示 socket 缺失与 input source fallback。
- 此项仅验证读取，保存/边界/下载和诊断内容脱敏仍待测。API token 字段不录入用户凭证。

### ST-002 — 缓存保留设置保存与边界
- 30 改为 7 保存成功，显示“设置已保存”，服务器展示 `retention.derived_days=7`；刷新返回设置后仍为 7。
- 输入 -1 与 0 时保存按钮禁用；输入 30 后重新启用。未观察到解释允许范围的提示。
- 已恢复并保存 30，页面确认 `derived_days=30`。
- 读取 spinbutton 的 Playwright evaluate 两次工具超时，但 AX 操作可正常执行；记为工具限制，不作为产品缺陷。

## 继续测试交接（2026-09-12）
- 服务 exec session `17038`，下轮先 poll 验证存活；不要因旧日志直接重启。
- CUA Chrome 测试 tab `850013371`，当前设置页；浏览器通过 `cua.getBrowser` 选中 ID `1`。须按工具入口规则重新获取有效 handle。
- 独立测试词典 UUID `9e9cd0ff-059b-4294-abab-428a3f8ad5ee`，名称 Phase1 中文・日本語 English，现有量子力学/duplicate/zh 词条 1 个。
- 下步：词典材料导入与确认/删除、模型与 IBus 非推理交互、诊断下载、导入错误与无模型校验；之后用明确标注的人工固定转录数据测试完整校对/导出，不调用模型。
- 目前只有部分空状态与词典/设置操作完成；全量目标保持 active。

### IB-001 — IBus 服务不可用与未加载状态
- 点击 IBus：显示“守护进程不可用”“模型未加载”，说明首次使用准备、60 秒空闲释放、启动预热关闭。
- 释放模型按钮禁用，默认语言自动/混合、平衡、模型 —；Portal 不可用时提示使用系统 ClassScribe Voice 输入源。
- 页面未出现可编辑语言/档位控制（当前为只读状态卡）。预热默认模型按钮可用，但按用户限制不点击模型加载入口。
- 当前隔离测试无 dictationd，因此真正桌面输入源集成仍需单独覆盖；本记录不把不可用状态当作集成通过。

### MO-001 — 模型列表与安装披露
- 模型页加载 20 张模型卡，显示语言/用途/限制/运行方式/窗口、安装状态、manifest、worker/策略阻塞原因与显存；全部未安装。
- FireRedASR2-LLM、TurboCTC、Voxtral、VibeVoice、Fun-ASR、Whisper reference 的准备安装禁用并显示原因；其他已实现且策略允许的模型可准备安装。
- MOSS-TD 点击准备安装出现确认区：1.71 GiB 下载/安装，3.76 GiB 需要空间，环境、许可证、确认有效时间、健康 WAV、语言与文字输入。
- 无已有健康录音时最终“确认安装并运行推理健康检查”禁用。按用户限制不执行该最终步骤。
- 无单独取消按钮；点击主导航导入可退出确认区。安装披露尚需 gated/强制对齐两类专门覆盖。

### ENV-002 — 服务终止、审批核查与恢复
- session 17038 已正常 shutdown、exit 143；该时段校验显示 Failed to fetch，日志没有 verify 请求，不能当作未安装校验结论。
- 首次重启被自动审批拒绝：担心 /install 恢复下载/推理。
- GitNexus 流程与 Serena 定义交叉确认：routes.py:245 的 /install → request_bundled_model_install → _request_model_install → ModelManager.request_user_install，仅生成内存 _plans；实际下载安装另需 /install/confirm，本轮从未点击或调用。
- 只读检查：隔离 cache 无文件，SQLite model_installations/jobs/job_checkpoints 各 0 行，无恢复任务。
- 携上述证据重新申请后获自动审批允许，服务恢复为 exec session 77378。

### MO-002 — 未安装校验与 gated 披露
- 服务恢复后 MOSS-TD 文件校验明确显示 `校验失败：model moss_td_0_9b has no active revision`；未启动健康推理。
- 点击未安装版本“回滚到此版”后 UI 无新反馈，原校验错误仍在；需结合响应核查，不以点击成功替代操作成功。
- pyannote 准备安装显示需要接受上游条款、CC-BY-4.0、派生 wespeaker 组件来源/许可证/完整 revision，以及未勾选条款框。未勾选或接受法律条款，未执行安装确认。

### F-003 — 模型回滚失败没有用户反馈（中）
- 无安装模型，点击 MOSS-TD“回滚到此版”：真实 API `/models/moss_td_0_9b/rollback` 返回 409，但卡片和页面不显示回滚失败。
- 离开再进入模型页清除其他操作提示后复测：仍无 alert/错误说明、按钮仍可点。与文件校验能显示明确错误形成区别。
- 预期：无可回滚版本时禁用并说明原因，或至少展示服务返回失败及恢复建议。否则用户无法知道回滚未发生。

### ENV-003 — 浏览器上传权限阻塞
- 新建 fixtures/terms.txt（多语言逐行词）与 material.md（测试讲义）。
- CUA Chrome filechooser 可打开，但 setFiles 返回 `Not allowed`，页面仍为 1 个原词条，无上传结果。工具调用异常等待约 19 分钟。
- 浏览器官方工具文档说明需用户在 Chrome 扩展详情启用 Allow access to file URLs；已告知用户。未自行变更扩展权限。
- 材料与音频浏览器上传暂未通过，不能归类为应用上传失败。继续执行不依赖文件权限的功能测试。

### ENV-004 — 文件权限恢复
- 用户明确告知已允许文件上传，Chrome 重连为 browser ID 2，同一 tab 850013371。
- session 77378 确认退出143后重启为 60944；重启前安装/任务/checkpoint 表仍全0，缓存仅回滚产生的0字节 .mutation.lock。
- TXT 浏览器 setFiles 重试成功，权限阻塞解除。

### GL-004 — TXT 材料导入
- 上传 fixtures/terms.txt 成功：原1条变4条，新增ニューラルネットワーク、人工智能、量子力学，来源 txt、建议、权重0.14，原中文 manual 量子力学保留。
- 本轮表单语言为默认日语，所有新建议都标 ja（包括中文）；Machine Learning 行未生成词条。需验证语言选择是否控制材料提取及英语分词阈值。
- 建议行只有删除按钮，未见“确认”或编辑入口。页面说明“确认后提升”却未提供直接确认操作，待以手工重录与产品合同核实。

### GL-005 — 材料重复选择、Markdown 与语言行为
- 切换 English 后重选同一 terms.txt，未发新 POST，词条不变；改选同内容 terms-copy.txt 则真实 POST 201，但词条仍不变（后端去重/提取规则待核查）。
- Serena 核对文件 input 的 onChange 后未清空 value，同文件重复选择不触发是明确前端行为。
- 中文模式上传 material.md：新增4条 zh/markdown 建议，包括整个中文句子与日语字符串；原手工 zh/量子力学/duplicate/1.00 未被低权重建议覆盖。
- 当前建议抽取不是语言自动检测；用户选择统一传入 X-ClassScribe-Language。界面无明显说明“词条表单的语言也控制材料导入”。

### F-004 — 材料建议缺少直接确认及编辑入口（中）
- TXT/MD 导入显示“建议”，页面承诺确认后提升，但每行仅删除按钮，无法直接确认/修改语言/读音/权重/alias。
- GitNexus 定位材料提取流程，Serena 检查 GlossaryPage 完整组件确认只有手工新增、删除、导入功能；用户手册描述的 alias/权重录入与建议确认没有对应控件。
- 可通过重新手工录入相同 canonical/语言实现覆盖，但不是可发现的建议确认流程；后续验证该替代操作是否保留语义与来源。

### F-005 — 同文件重选不触发材料导入（低）
- 在同一词典页面导入 terms.txt 后切换语言，再重选 terms.txt，没有请求也无提示；不同文件名会发 POST。
- 文件 input 未在处理后复位；影响失败后的同文件重试及切换词典/语言后重用同一材料。跨词典影响需再实测。

### UP-001 / F-006 — 文件选择后列表仍为空、无法入队（高）
- 在词典上传已经成功的同一已授权 Chrome 中，导入页多选 broken.wav + empty.wav：选择接口成功返回，但待上传列表为空、入队按钮禁用，无错误提示。
- 单选标准 PCM WAV `silence-2s.wav`（16kHz/mono/16bit/2s）复测：同样列表为空、按钮禁用，排除只是不支持损坏/空文件。
- GitNexus 定位 UploadPage，Serena 核对 chooseFile：setFiles 的 updater 内延后读取 event.target.files，随后立即 event.target.value=''。该时序与文件被清空吻合（代码因果为推断，UI失败已实测）。
- 预期：有效文件显示在待上传列表，并允许继续配置/提交；实际主导入入口被阻断。刷新后和独立入口仍需进一步复测。
- 此处尚未向 API 提交音频、更未启动任何模型；损坏/空文件后端校验仍待覆盖。

### UP-002 — 刷新后复现文件选择丢失
- 完整刷新首页后再单选 silence-2s.wav，仍未显示文件列表，复现 F-006。
- 390×844 viewport 初步截图显示页面整体缩小；DOM innerWidth=390、clientWidth/scrollWidth=375，无横向溢出。截图比例与展示有待更细视觉核查，不把此次观察算作响应式通过。已 reset viewport。

## 最新交接补充
- 当前服务 session 60944，需先 poll 判断存活；旧 17038/77378 均已终止。
- 浏览器现 ID 2，tab 850013371，当前导入页；用户已明确开启上传权限，TXT/MD实际成功，ENV-003 已解除。
- 本轮新增 F-003 模型回滚409无反馈；F-004 建议确认/编辑入口缺失；F-005 同材料文件重选不触发；F-006 有效音频选择后列表为空（高，刷新可复现）。
- 词典现8条，含手工zh量子力学与TXT/MD建议；没有删除用户数据，没有安装模型或调用安装确认。
- 导入入口当前失败，但后续应继续测试其他控件、人工固定转录数据下的校对与导出，不因此把全量目标缩小。损坏音频后端校验和非空任务页面尚未完成。

### DATA-001 — 非推理固定转录数据
- 用 fixtures/seed_transcript.py 在隔离测试库创建已完成任务 `20000000-0000-4000-8000-000000000001`，录音 `10000000-0000-4000-8000-000000000001`，12秒静音，仅用于播放控件与时间轴。
- 三段固定中/日/英文本、teacher/student两说话人、0.95/0.35/0.82置信度、对齐/粗时间状态、候选与token来源。标题及来源明确标注人工测试，不冒充真实模型结果。
- 通过直接测试数据构建绕开尚未工作的上传入口；后续仅可证明转录页/导出消费已有数据的行为，不证明上传到推理流水线成功。
- 首次seed因枚举名称AUTO错误回滚，改用源码真实AUTO_MIXED后成功；这是测试脚本错误，不计产品缺陷。没有创建待运行任务或调用模型。

### TR-001 — 非空稿件列表与打开
- session60944退出143，恢复为12202；恢复后真实列表显示1份人工测试稿、12秒、已完成，翻页禁用，点击可进入转录工作台。
- 标题、多语言3句、95%/35%/82%质量、teacher/student轨、ZH/JA/EN轨与12秒音频正确呈现；默认智能纠正层显示英文 `$12.`。
- 原始/忠实/智能纠正/用户版、仅低置信、播放、±10秒、6种速度均有入口。候选面板未选择时有提示。
- 列表语言直接显示内部值 auto_mixed，属于用户措辞待改善（未本地化）。

### TR-002 — 文本层、低置信筛选与自动保存
- 英文原始=无句点 12 dollars，忠实=有句点 12 dollars.，智能=$12.，未编辑用户版回退智能，四层行为通过。
- 勾选仅低置信后服务器更新最终仅留下35%日语句；95%句按钮0个。点击该句播放位置定位到4秒且不自动播放。
- 同时说话人/语言轨也只剩student/JA（原teacher与ZH/EN被隐藏），与手册“低置信筛选不隐藏时间轴证据”的描述存在差异，待截图确认实际时间轨影响。
- 用户文本改为 `これは手動校正のテストです。`，等待自动保存后显示“已自动保存”，用户版列表同步更新，模型候选仍为原文。刷新持久化/撤销重做/并发仍待测。

### TR-003 — 撤销/重做、说话人改名与审计
- 撤销恢复日语原回退文本，重做恢复人工校正文本。
- 说话人student改为“学生・佐藤”并保存，句段列表同步更新。
- 展开审计历史，实际出现segment_text_edited、segment_edit_undone、segment_edit_redone、speaker_display_name_changed四项，human/local-user、前后文本/版本和student原ID清楚可追踪。
- 以上为真实API与数据库操作，未使用模拟响应。

### TR-004 — 候选采用、刷新与播放
- 点击采用候选将日语用户文本恢复为候选“これはテストです。”；刷新后重新打开，学生・佐藤名称与候选文本仍保存。
- 0秒后退10秒停留0；0秒前进到10；再次前进封顶12秒。选择2×播放，从2秒播放至12秒结束，按钮回到“播放”。
- 音频为测试静音，此项只验证媒体加载、时间与按钮状态，不评估实际声音质量。暂停、其他速度、波形定位仍待覆盖。

### EX-002 — 用户版 Markdown 生成与下载请求
- 全选本页1份，选择用户版/MD/逐句，生成反馈成功1失败0，卡片显示204 bytes、SHA前缀69240b351b82。
- 点击下载后真实 GET /exports/db0cf5c1-c48a-4f5d-a2d6-f979c86733cf 返回200；浏览器下载事件工具超时且无UI错误，浏览器落地下载仍需核查，不能仅以200宣称下载完整成功。
- Markdown服务器产物逐行核对：三段语言完整，时间0/4/8秒、学生・佐藤名称正确，英文未编辑user层回退到smart `$12.`。

### EX-003 — SRT/VTT 生成（内容核查进行中）
- 用户版SRT生成成功1失败0，182 bytes，SHA 78b07d8e5cd2；随后从同一页面生成VTT。
- SRT与VTT实际内容均为3条，0–4/4–8/8–12秒，无重叠/负时间，三语文本与用户版回退正确。VTT头为WEBVTT，SRT序号1–3，毫秒分隔符合各格式。
- JSON用户版成功3331bytes；TXT忠实版/段落已发起并进行产物核查。

### EX-004 — 六格式生成与产物核对
- TXT忠实/段落成功93bytes，实际英文12 dollars.；JSON用户/逐句包含三条、requested_layer/resolved_layer（smart/user/smart）和candidate provenance，学生・佐藤名称正确。
- CSV智能/逐句成功484bytes，页面六个产物卡齐全；全选后清空选择使生成0份按钮禁用。
- 六个服务器产物已复制至 evidence/exports/，包含SHA对应文件以便复查。此为服务器输出证据，不冒充浏览器下载文件。
- 粗时间日语段在SRT/VTT与JSON记录未见timing_quality/coarse标记；需要结合正式导出合同/代码确认是否缺失，不以人工样例直接下最终结论。
- 浏览器下载GET200后download事件超时；尝试chrome://downloads被浏览器URL安全策略明确禁止，未绕过。浏览器最终落地仍为未验证项。

## 当前交接（追加）
- 上轮为有进展：新增真实转录页与六格式导出证据，未完成全量目标。
- 服务session12202（TTY）；先poll后决定是否恢复。12202之前60944已exit143。
- 固定已完成任务20000000-0000-4000-8000-000000000001；三段30000000-...-000001/2/3；日语user当前采用候选，speaker显示学生・佐藤。
- 当前tab850013371/browser2在导出页，6个服务器产物已生成，选择已清空。
- 还需：导入配置全部组合/模型选择器、词典更多边界/删除、诊断下载、历史多页/多任务、转录编辑失败/并发/空文/跳转保存、媒体暂停/其他速度/视觉、任务详情控制与桌面集成。浏览器下载最终落地因工具限制未验证。

## 2026-09-13 恢复测试
- 用户明确“继续测试”；HEAD仍c19261da，原session12202仍存活，未重启服务。上一轮暂停期间未测试。继续既有范围，仍禁模型推理。

### UP-003 — 严格模式、语言过滤与模型选单
- 日语+严格单模型+自动最佳时提示必须指定主模型，辅助模型说明明确严格仅约束正文ASR。
- 选单仅展示日语兼容正文：Granite4.1、Qwen1.7/0.6、ARK、Nemotron3.5，均标待安装/检查；不出现FireRed中文专用模型。
- 选择Granite后选单自动关闭；切换中文后不兼容选择被清为自动最佳，重新提示指定主模型。
- 中文选单包含FireRed AED、Qwen两档、ARK、Nemotron，不包含Granite4.1。Escape可关闭选单。
- 本项只操作选择与配置，不提交任务、不加载模型。

### UP-004 — 输出选项、档位与导入草稿保留
- 六格式和忠实/智能两层可全部取消，也可逐个重新选择；无文件时入队禁用，故尚不能据此证明空格式提交校验（F-006阻断文件路径）。
- 快速、最高精度radio均可切换；English说明只显示英语正文MOSS Preview、去掉必需LID，保留中英标点辅助。
- 将语言设English、词典选测试词典、人数5+、最高精度、全格式，然后导航到任务队列再返回：重置为auto/不使用/自动人数/平衡及默认输出。
- 导入草稿配置不跨页面保留，用户去词典/模型页查看后需重填，记录为可用性问题F-007（中）；已有导入进度却仍保留，状态保留不一致。

### GL-006 — 用户手动确认删除词典
- 删除按钮触发confirm弹窗，工具click/dismiss曾超时，不能推断取消成功。用户明确告知亲自点击确认，随后UI显示“创建第一个课程词典”。
- 只读数据库核查：glossaries=0、glossary_terms=0；已有人工任务1、转录段3、导出6保留。未受影响数据初步验证通过。
- 旧词典目录仍存在，正在核查材料文件是否清理；不能仅依据UI消失宣称删除完整。
- 中断的“重录人工智能建议”操作未获成功证据，不记为通过。
- 旧词典目录下find未发现任何文件，导入页课程词典仅剩“不使用”，材料文件清理与跨页列表更新通过；仅遗留空目录，不包含材料数据。

### DATA-002 / HI-001 — 31份稿件分页
- fixtures/seed_pagination.py增加30份明确标人工的已完成任务，无pipeline/model调用；总31份，只有31号和原始稿有正文，其他用于空正文状态。
- 历史第一页恰30个稿件按钮，按31→02倒序，上一页禁用；第二页仅原始稿1条，共31，下一页禁用，可返回第一页。
- 已完成但无正文任务仍标“查看稿件”，后续检查打开后的空状态。

### TR-005 — 已完成空正文
- 打开人工30号（无segments）后2秒音频正常，句段列表显示“该筛选下没有句段”，侧栏提示选择一句。无崩溃。
- 此时未启用筛选，该文案不能区分无转录内容与筛选无匹配，记录为低影响措辞问题；人工构造的已完成空正文不证明实际流水线可产生该状态。

### EX-005 — 跨页选择、跳过空正文与批量生成
- 导出第一页30条中仅31号有正文可勾选；其余29条禁用且明确“暂无可导出的正文”。全选本页只选1份。
- 翻第二页后仍保留已选1份，再全选末页变2份；批量MD智能/逐句成功2失败0，分别生成67bytes的31号与204bytes原稿两独立文件。
- 证明跨页选择与空正文保护正常，不以空白导出伪造成功。

## 最新恢复交接
- 服务仍session12202，Chrome browser2/tab850013371，导出页第2页，已选2份，最近批量成功。
- 用户亲自确认删除测试词典；现无词典/词条/材料文件，原空目录尚在。不要把此前工具取消尝试记为取消成功。
- 总31份人工已完成任务，无运行任务、无模型；只有原始稿与31号具正文。
- 本轮新增UP-003/004、GL-006、HI-001、TR-005、EX-005，F-007为离开导入页配置重置。全量覆盖仍未完成。

### ST-003 — 诊断刷新与诊断包请求
- 点击刷新后generated_at由2026-09-13T01:16:04变01:16:13，真实GET diagnostics返回200，内容更新。
- 点击下载脱敏诊断包，GET diagnostics/bundle返回200，无页面错误；隔离根未产生诊断文件（可能为内存响应），浏览器下载落地仍未验证。不绕过此前下载管理页安全限制。
- 诊断仍无model/worker/GPU lease，符合无模型测试约束。

### TR-006 / F-008 — 无历史撤销/重做静默失败（中）
- 31号无任何人工编辑历史，撤销与重做均可点，真实API各返回409；页面无alert/原因，原“已自动保存”仍显示。
- 预期无可撤销/重做时禁用按钮或提示无历史，而非静默失败。
- 无候选/token时分别标0，无采用按钮，空证据状态正常。
- fill('')后文本未变化，改用真实键盘全选/Backspace，文本清空并进入等待保存；工具操作差异不计产品缺陷，最终空文本保存结果继续验证。

### TR-007 — 空文本及离页自动保存
- 键盘全选Backspace清空后API保存成功，用户版列表明确显示“空句段”，编辑框为空、已自动保存。未编辑null回退与显式空字符串有区别，后续需验证导出一致性。
- 输入“离页自动保存测试：中文・日本語・English 😀”后紧接点击词典，再返回31号稿件用户版，文本完整保留。证明此次快速跨页保存成功，Unicode与Emoji未损坏。

### QU-002 / JO-001 — 非空暂停队列、任务详情与取消
- 将明确人工30号在测试库设为paused/created/0，不调用pipeline；队列出现已暂停0%，查看任务/稿件/恢复到队尾/取消此课。
- 查看任务进入此前未覆盖详情页：已暂停、等待调度、SSE连接正常，继续/取消可用，重试/稿件/导出禁用（无正文）。流程10阶段等待，技术详情可展开为activity/runtime/event空状态。
- 点击取消后真实状态收敛“已取消”，当前操作任务已取消，SSE明确停止，实时详情记录请求取消。未点击继续或重跑，未加载模型。
- 已取消状态暂停/取消/重试按钮均禁用，返回队列显示清空，终态控制与活跃队列移除通过。

## 本轮交接
- session12202仍在，browser2/tab850013371当前队列空状态。
- 人工31号user文本为“离页自动保存测试：中文・日本語・English 😀”；人工30号已通过UI取消，余任务仍completed；无推理执行。
- 新增F-008无历史撤销/重做409无提示；ST-003/TR-006/TR-007/QU-002/JO-001见上。
- 仍未覆盖：并发保存冲突与恢复、网络错误草稿、空用户文本导出一致性、其余播放/视觉/键盘、上传F-006之外后端错误、模型已安装非推理管理、桌面集成。不得标完整测试完成。

### TR-008 — 双窗口乐观锁测试（进行中）
- 同时打开31号句段两个窗口（原850013371、新850013822），均加载原用户文本；窗口A编辑“窗口A已保存的内容”，只读DB确认version4。
- 窗口B在旧版本上提交“窗口B的冲突草稿，必须保留”，正在核对冲突提示/草稿保留/恢复动作。
- B提示保存失败、草稿保留，A数据库仍version4，未被覆盖。点击重试仍失败；导航到词典保留全局草稿失败条，回到稿件仍完整保留B文本，但再次重试也不能保存。
- 缺少针对版本冲突的原因解释/加载最新并合并/放弃草稿入口；只有重试按钮不能解决冲突。记录F-009（中）：乐观锁保护有效，但冲突恢复流程不完整。页面尚无证据证明刷新后草稿持久。

### EX-006 / F-010 — 显式清空用户文本的显示与导出不一致（中）
- 窗口A将31号用户文本清空，DB version5、user_text=''，转录用户版显示“空句段”（TR-007同路径）。
- 页面选31号JSON/用户版/逐句导出成功716bytes；服务器产物27f535c9-02df-4c6d-9bdf-d23087750214.json中user_text=''，但resolved_layer=smart、text='跨页测试。'。
- 用户已清掉的正文在“用户版”导出重新出现，与编辑器显示不一致。手册允许user空回退，但编辑UI未呈现同一规则；需统一显式空与未编辑null的含义或明确告知回退。
- 产物已留存evidence/exports/，未以空导出作为通过。缺陷严重度按显示/导出不一致记中，不假设未证实的数据丢失。

### UI-002 / F-011 — 低置信筛选隐藏其他时间轨证据（中）
- 三语原稿截图：未筛选说话人与语言轨覆盖0–4 teacher/ZH、4–8学生/JA、8–12teacher/EN。
- 勾选仅低置信后截图实际仅保留中间4–8秒学生/JA色条，前后色条消失；剩余条位置仍正确，不是缩放或时间偏移。
- 与user-guide“低置信筛选只影响显示，不隐藏时间轴证据”不一致；建议正文筛选保留完整时间轴或显式说明隐藏。截图已在本轮工具记录展示，未虚构本地PNG路径。

### ENV-005 — 响应式工具尺寸未生效
- 请求viewport768×900后截图仍2048桌面布局，DOM实测innerWidth/clientWidth/scrollWidth=2048、height977，不能宣称平板测试通过。
- 已reset，保留响应式验证待办；本次是工具未生效，不记应用响应式缺陷。

## 最新交接补充
- 第一tab850013371在三语原稿，低置信筛选开启；第二tab850013822在31号冲突编辑，保留B草稿“窗口B的冲突草稿，必须保留”，重试仍409。不要把冲突草稿覆盖或丢弃当作恢复成功。
- 31号服务器文本当前显式空字符串、version5；窗口A先保存内容后清空。JSON用户版实际回退smart，已记录F-010并留存产物。
- 本轮F-009冲突恢复不完整、F-010空用户文本导出不一致、F-011过滤隐藏其他时间轨；无模型调用。
- 下一步优先补：冲突刷新草稿、网络故障恢复、上传错误验证、模型健康WAV选择（不确认推理）、词典CSV与错误响应、媒体暂停、桌面入口、可信尺寸的视觉QA。

### GL-007 / F-012 — 非UTF-8材料500且界面无反馈（中）
- 新建测试词典Phase1材料边界（f08b4cfc-cc55-476a-bfa8-1ed9361c9bb8）。通过实际文件选择上传invalid-encoding.txt（FF FE 00 FF）。
- /documents返回500，服务异常链routes.py:321→service.py:1382→importers.py:50，read_text(utf-8)抛UnicodeDecodeError未转成可理解错误。
- 页面仍0词条，没有错误/失败提示。预期拒绝并提示需要UTF-8或可支持编码，不静默产生服务器500。

### GL-008 — 空材料、失败恢复与建议手工提升
- empty-material.txt（0字节）返回201，但0条、无成功/无提取内容提示；与失败时无提示相似，用户无法分辨，记录可用性问题。
- 随后导入正常terms.txt成功3条ja/txt建议，证明500后页面仍能继续上传。
- 手工录入相同人工智能/ja、读音じんこうちのう后，原建议变manual/已确认/1.00、数量仍3条；可作为确认替代操作，但会把来源从txt改为manual，支持F-004确认入口不清晰结论。

### MO-003 — 强制对齐健康WAV前置表单
- Qwen ForcedAligner准备安装正常显示条件；人工seed录音未列入健康下拉（不冒充server验证录音）。
- 上传broken.wav前端显示明确alert“健康录音必须是完整的RIFF/WAVE文件”，确认按钮禁用。
- 随后上传有效silence-2s.wav成功，自动选中32000samples，错误清除。证明同一浏览器真实WAV上传通路可工作，F-006仅导入页选择路径仍有问题。
- 此时“精确文字（强制对齐模型必填）”为空，最终确认按钮却可用；记录前置表单提示/启用状态不一致，尚未点击确认，不能断言服务端缺失校验。健康静音仅用于格式校验，不作为有效推理健康音频。
- 本轮未点击任何“确认安装并运行推理健康检查”，无下载/模型加载。

## 本轮材料/健康表单交接
- 主tab850013371已退出安装确认到词典。新增词典f08b4cfc-cc55-476a-bfa8-1ed9361c9bb8有3条，其中人工智能已手工提升；非UTF-8错误F-012。
- 有效silence-2s.wav经真实健康上传接口登记，未创建课堂任务；只生成ForcedAligner内存准备计划，未调用install/confirm。
- 第二tab850013822仍保留版本冲突草稿。session12202继续运行。
- 已测材料非UTF-8/空文件/恢复与强制对齐健康录音格式。剩余条目仍以上轮待办为准，CSV/网络失败恢复/桌面/更多视觉尚需继续。

### GL-009 / F-013 — CSV确认词条导致整份导入500（中）
- 按import_csv_terms支持字段构造带UTF-8 BOM的terms-valid.csv，含canonical/reading/aliases/language/weight/confirmation/chapter；一条confirmed0.9、一条suggested0.9。
- 浏览器上传后未增加词条，无UI失败提示。服务异常service.py:1397→TerminologyRepository.record_suggestions:48，ValueError“suggestion import accepts only low-weight unconfirmed terms”。
- CSV解析器明确支持confirmed并保留其权重，但Web材料入口统一走record_suggestions拒绝确认词条，前后合同冲突。预期支持CSV已确认导入，或给出明确受支持格式/字段错误，而非500。
- 整份文件未产生新词条；正在用仅suggested版本区分正常CSV路径与确认字段问题。

### GL-010 — 建议CSV、BOM、语言和权重
- 将两条均设suggested后导入成功：中文/en逐行语言覆盖表单默认ja；0.9建议权重均限制0.30，UTF-8 BOM正常处理，原3条保留，合计5条。
- 缺canonical列返回500（ValueError course-term CSV requires a canonical column），UI无alert；非法weight返回500（float转换ValueError）。并入F-012输入错误未转换为可见验证反馈，扩展复现文件见fixtures。
- 只读DB确认CSV aliases数组保存两条/一条，与输入一致；页面未提供alias展示编辑（F-004）。chapter无对应glossary_terms列，未追踪其他持久位置，不宣称章节已保存。

### DESK-001 — 桌面集成测试环境核查
- 当前PATH只有ibus，没有classscribe/classscribe-core/classscribe-doctor/classscribe-dictationd。
- 系统与用户applications、ibus/component、systemd/user目录均未找到ClassScribe安装项。源码packaging包含desktop/launcher/service，但不等于实机安装成功。
- 源码desktop入口依赖/usr/libexec/classscribe-launcher和用户classscribe-core.service；未执行系统安装或启用服务，以免操作非隔离数据/触发模型预热。
- 桌面菜单、IBus输入源、全局快捷键实机非推理交互暂缺安装环境证据，保持未完成，不能用WebUI代替。

## CSV与桌面核查交接
- 主tab850013371仍材料词典，5条；最后上传非法weight返回500。F-013已确认CSV导入500，F-012扩展缺列/无效weight。
- session12202仍运行。第二tab保留冲突草稿。未改产品源码或安装系统服务，未模型推理。
- 后续优先细化所有剩余UI控件清单，测试手工字段边界/保存错误恢复，处理真实桌面与响应式验证环境限制；目标不能标完成。

### KB-001 — 模型选单键盘焦点
- 打开弹窗焦点在自动最佳；Shift+Tab两次从自动最佳→关闭→末项Nemotron，焦点仍在dialog；末项Tab回关闭按钮，焦点循环正常。
- Escape关闭后焦点回主模型触发按钮，便于键盘继续操作。未选择或加载模型。

### GL-011 — 名称长度边界与长名称布局
- 257个中文字名称提交返回422，前端没有alert或长度说明，输入保留；属于词典创建错误未显示（与F-002静默验证同类，扩展复現）。
- 改为256字成功创建，达到API允许上限；长名称可换行，无文字重叠，但详情右侧导入按钮被挤成窄列分6行，左列表高度大幅增加。记录低影响布局问题F-014：允许的长名称挤压主要操作按钮。
- 这两个测试只创建隔离词典，未修改原5条材料边界词典。

### GL-012 / F-015 — 大写.CSV被误按TXT抽取（中）
- 将已验证的terms-suggested.csv原样复制为terms-uppercase.CSV，在空的256字名称词典上传。
- 实际新增6个ja/txt/0.14建议：建议别名、建议词、确认别名、确认别名二、确认词、第一章；而非原本2个CSV词条。读音、语言、alias关系和权重丢失，章节文字被当术语。
- 同字节小写.csv已成功按2条解析（GL-010），唯一区别扩展名大小写；与GlossaryPage的case-sensitive endsWith('.csv')吻合。预期扩展名不区分大小写或明确拒绝，不能静默换解析器。

### GL-013 — 跨词典同文件重选复现F-005
- 在长名称词典导入terms-uppercase.CSV后切换到Phase1材料边界，再选择同一文件，仍5条无变化；服务日志只有长名称词典的POST，无材料边界新POST。
- 证明F-005还影响把同一讲义分别导入不同课程词典；不是后端同词典内容去重。

## 自主范围关闭状态（2026-09-13更新）
- [ ] 导入页文件列表链：F-006阻断；人工选择/拖放交叉验证、列表排序删除、批次停止与重试按用户要求暂时跳过。真实上传接口的成功/错误/清理/幂等已补测。
- [x] 队列/详情：人工状态覆盖运行、暂停、取消、失败、完成、空队列和全局暂停；会启动执行器的继续/重试排除。
- [x] 转录：多层、筛选、播放/暂停/倍速/定位、编辑、离页保存、并发冲突、暂时故障、撤销重做、候选、改名、审计和刷新均已覆盖；片段重跑排除。
- [x] 导出：六格式、三层回退、逐句/段落、跨页、部分失败恢复、重复生成、长字幕/保护单位/粗时间均已覆盖；浏览器下载落地按用户要求跳过。
- [x] 模型：列表、选择器、披露、gated/aligner前置、健康WAV边界、未安装/损坏/环境缺失/回滚均已覆盖；副本删除需确认，修复/最终健康确认会安装或推理，均跳过。
- [x] 设置：数值/开关/持久化/token错误恢复、诊断刷新、ZIP内容/鉴权/脱敏均已覆盖；三层数据清理入口不存在（F-033）。
- [x] 全局UI：桌面、768和390布局、弹窗键盘焦点、品牌/导航/刷新与滚动状态已覆盖；已记录对应问题。
- [ ] 桌面/IBus原生：本机未安装desktop、component、user service，且需要人工桌面环境；按用户要求跳过。真实语音转写始终排除。
- [ ] RPM安装升级卸载生命周期：当前`rpm --initdb`无法在测试临时目录创建锁，按环境权限项跳过；静态RPM/desktop检查已有通过证据。
- [ ] Pyright：配置指向`.venv`，但可执行工具未安装；按额外环境项跳过。规范Mypy/Ruff/编译检查已通过。

## 本轮交接
- 主tab850013371在材料边界词典5条；新增长名称词典UUID9b3ea6cf-a989-41f7-a8c0-b99ad397588a有6条误按TXT提取数据。
- F-014长名称挤压按钮、F-015大写CSV误解析；KB-001键盘选单通过；F-005跨词典复现。
- 第二tab850013822继续保留冲突草稿，session12202存活，未调用模型。

### ST-004 — 数值细边界与保留开关
- 1.5与空保留天数禁用保存，3651仍可用（未提交，不把未知上限当缺陷）；1天成功保存。
- 在无dictationd/无模型隔离环境勾选保存IBus原始音频，API展示ibus.save_audio=true；切页再进入仍勾选、1天保持，设置持久化通过。本项不证明实际录音落地功能。
- 随后恢复30天、取消保存音频并保存，待最后确认UI；从未启动录音或推理。
- ST-004最后UI确认derived_days=30、ibus.save_audio=false、设置已保存，恢复完成。

### MEDIA-001 — 暂停、倍速选项与波形定位
- 0.5×播放时按钮变暂停，点击暂停后停在4.574秒；后续检查时间仍4.574，确认暂停实际生效。
- 0.75/1/1.25/1.5各档通过DOM选中状态逐个核对，2×实际播放已在TR-004通过；不把仅选择状态当成所有倍率精确计时测量。
- 按截图点击12秒波形中点，实际定位到6.000秒，仍显示播放（未自动播放），与页面说明一致。
- 播放内容为静音测试素材，本轮未启动模型。

### TR-009 / F-016 — 候选证据展示不足（中）
- 展开Token provenance：显示token文本/0.95，DOM提示包含candidate_id、manual_fixture来源，基本token来源可查。
- 候选卡实际DOM仅模型名、95%、normalized正文、重复模型ID和采用按钮，无其他详情入口/tooltip；未展示已在固定数据中提供的完整model_revision、decode_config或raw候选文本。
- 与用户手册“点击句段可查看完整revision、decode参数和原始置信度”的承诺不符；当前只有一个置信度百分比且不能区分原始/校准。记录为证据可见性缺口，不冒充模型真实证据测试。

## 设置/媒体交接
- 30天/ibus.save_audio=false已恢复并确认。媒体暂停、六档选中、半速实际播放、中点6秒定位已测。
- 第一tab在原始稿第一句，token来源展开；第二tab仍冲突草稿。session12202未重启，无模型。
- 新F-016候选详情缺失；完整目标仍需剩余清单中的真实上传、错误恢复、导出边界与桌面/视觉覆盖。

### EX-007 — 批量部分失败与权限故障
- 仅对人工31号exports目录临时chmod500（原700），实际页面跨页选择31号与原稿，生成MD智能版。
- UI准确报告成功1失败1，成功稿保留下载卡；失败稿指出31号文件名与“500 Internal Server Error”，没有误报整批成功。
- 错误提示未解释目录不可写/恢复办法，记录恢复提示不足（低）；故障本身为刻意注入，不算正常配置必现。
- 已立即恢复目录700；接下来验证用户再次生成可以恢复。
- 恢复后重试同一批次成功2失败0，旧alert清除，31号重新生成67bytes，原稿204bytes；重复生成保留独立卡片，同文件名/同SHA重复但不混淆到另一任务。
- stat再次确认exports权限700，故障注入完全恢复。

### TR-010 — 临时存储故障后草稿重试
- 对隔离SQLite建立BEGIN IMMEDIATE事务锁（session41701，不改数据），31号在version5上编辑“数据库忙后恢复保存的测试文本”。
- 真实PATCH因database is locked失败，页面明确“文本保存失败，草稿已保留”，文本完整保留。
- 已向锁进程输入换行执行rollback/close，session41701 exit0且明确lock released。随后点击重试保存，DB version6且文本正确，说明普通暂时错误可恢复；与F-009版本冲突不可恢复应分开。
- 注意服务异常堆栈包含SQL参数中的转录正文，违背用户手册日志不记录正文承诺：F-017（中，隐私日志）。本轮只有人工测试文字，不含真实用户音频/正文。
- TR-010最终UI已自动保存、全局失败条消失，正文与DB一致，恢复通过。

### ENV-006 — 新标签也未应用视口覆盖
- 重新获取browser2 viewport capability，设置768×900后创建新tab850013825；实测width2048/height1096/client2033，说明不是旧tab缓存导致。
- 已reset并关闭临时tab。当前工具路径无法提供可信指定宽度，不再重复同样尝试；响应式测试仍缺真实尺寸证据。

## 故障恢复测试交接
- EX-007部分导出失败/恢复通过；31号exports目录权限已回700。
- TR-010数据库锁错误/重试恢复通过；锁session41701已exit0，无遗留事务。
- F-017服务异常日志暴露正文参数。31号现user文本“数据库忙后恢复保存的测试文本”、version6。
- 主tab仍31号编辑已保存；第二tab保持旧冲突草稿；session12202运行，无模型调用。

### EX-008 / F-018 — 粗时间状态未随导出提供（中）
- 结合之前真实导出的日语structure时间段SRT/VTT/JSON未含coarse标记，核对user-guide:93–94明确承诺精细对齐失败导出标记coarse timing。
- GitNexus定位render_export流程，Serena核对ExportSegment结构没有timing_quality/coarse字段，导出数据结构无法携带该状态；现有产物也未显示。
- 结论：导出缺少时间精度标记，与合同不符。仅证明已有粗时间记录的消费行为，不将人工fixture当实际对齐失败过程。

### EX-009 — 长用户文本字幕边界
- 31号用户层输入204字符人工长文本（含12 kg、20°C、中文标点，重复3次）并自动保存，通过UI生成用户版SRT。
- 产物94b084f0-9c79-4b52-a6b9-62bc0da74a76.srt只有1条0–2秒cue、2行，忽略空白后与204字符输入完全一致，数字/单位未被拆开，本样例不丢字/不负时间/不重叠。
- 每行约100字、2秒显示204字不可读；此样例刻意超出正常语速，且无用户层精细token时间。记录边界行为：保留内容但未给可读性/粗时间告警，不把“最多两行”当可读性通过。
- 文件已复制evidence/exports，未真实重对齐或运行模型。

### TR-011 / F-019 — 保存失败草稿刷新后丢失（高）
- 第二窗口仍显示保存失败、B冲突草稿保留时执行浏览器reload；刷新后失败条消失，重新打开同一句编辑框仅有服务器最新长字幕文本，B草稿无恢复入口。
- 本次工具流程未观察到beforeunload弹窗；为避免仅凭自动化推断，GitNexus定位draft状态并Serena确认useTranscriptSaves仅内存create({drafts:{}})，全文搜索frontend/src无beforeunload/pagehide/visibilitychange处理。
- 因而“草稿已保留”只在当前页面生命周期有效，刷新后未保存文字丢失。应提供持久草稿/明确离页保护和冲突恢复，不能让通用“保留”提示造成持久性误解。
- 仅丢失人工B测试草稿，无真实用户内容。第二tab目前无未保存草稿。

## 字幕与刷新交接
- 31号用户文本为204字符长字幕测试文本，已保存；SRT留存evidence/exports。
- F-018粗时间标记缺失、F-019刷新丢失失败草稿；EX-009长字幕不丢字但不可读。
- 第二tab已无需要保留的冲突现场，可关闭；主tab当前导出页面。无模型调用，session12202运行。

### API-UP-001 — 真实上传接口补充验证（不替代浏览器）
- fixtures/probe_upload_api.py使用项目已有ASGI request helper调用真实/recordings/{id}/upload，真实FFprobe；不启用lifespan、不创建job、不运行模型。TestClient缺httpx2后改用项目既有helper，无新增依赖。
- empty.wav/broken.wav返回409 MEDIA_PROBE_FAILED，物理路径被替换为[LOCAL_PATH]；terms.txt返回409 UNSUPPORTED_MEDIA。
- 有效2秒WAV返回200、32000samples/16kHz/mono及upload_validated=true。同UUID重传返回同id/hash，不新建录音。
- 完整响应已保存evidence/upload-api.json。不能证明F-006阻断后的浏览器错误提示、批次重试或排序正常。
- API-UP-001清理核查：失败101/102/103均0录音行、0文件，成功104为1行1文件。

### API-JO-001 — 缺模型前置拒绝（接口补充）
- GitNexus/Serena确认create_job在持久化/调度前执行preflight，仅读已安装记录；隔离环境无安装模型。
- 对有效录音104请求中文balanced/auto_best/MD任务，真实API返回409 MODEL_NOT_FULLY_INSTALLED，明确缺firered_asr2_aed、firered_vad、moss_td_0_9b，没有进入调度或启动模型。
- 这是前置安全路径验证，不替代UI提交后错误卡/重试流程。

## 覆盖审计结果（仍不完成）
- 已取得全部Web主导航页、转录/任务详情的基本状态与大量真实读写证据；原目标尚缺关键上传交互链、可靠响应式/桌面实机、浏览器下载落地等证据。
- 上传F-006在工具选择路径多次复现，同浏览器健康WAV与材料上传成功；需要用户手动选择作独立交叉验证，才能区分真实用户事件与自动化特有时序。
- 不将接口补充、源码审计或人工fixture范围扩称为完整用户测试。

### API-ST-001 — 诊断包真实响应内容（补充证据）
- 直接调用真实diagnostics/bundle ASGI路由，未查询浏览器下载管理页，验证的是响应内容而非浏览器落地。
- 返回200、application/zip、attachment文件名classscribe-diagnostics.zip；ZIP仅diagnostics.json，JSON可解析。
- 本次快照未含home路径、专用测试token或三种已知人工正文；仅据此证明该样例未泄露，不宣称所有输入的脱敏充分。
- 响应ZIP及检查结果已存evidence/diagnostics-response.zip、diagnostics-response-check.json。未运行模型。

### API-ST-002 — 脱敏与鉴权的控制样例
- 执行项目已有test_authenticated_diagnostic_bundle_contains_only_redacted_json，在单独/tmp测试库注入home与private课堂正文；未鉴权401、鉴权ZIP200、home及raw_text被脱敏，断言通过。
- 这是有控制输入的接口补充验证，不替代浏览器下载完成证据；不执行模型。

### F-020 — 诊断包遗漏页面IBus/Portal状态（低）
- 页面diagnostics含ibus.dictationd/portal/fallback，bundle ZIP JSON键只有system/gpu/components/workers/models/lease/queue/errors/metrics等，无ibus。
- Serena确认GET diagnostics会追加await _ibus_status(runtime_paths)，bundle只调用diagnostic_bundle_bytes(provide_diagnostics())，没有追加；因此不是本次进程环境差异。
- 用户导出诊断包排查IBus时无法携带页面已显示的daemon/Portal失败原因。建议导出与页面使用一致脱敏快照。

### ENV-007 / UI-003 — 视口现已真实变为768×900
- 只读检查发现主tab侧栏缩为图标，DOM确认innerWidth768/height900、clientWidth=scrollWidth753，截图同宽；此前ENV-005/006是当时未生效，不能继续作为永久阻塞。
- 尚不确定是延迟生效还是外部改变，不猜测原因。基于当前实测开始可信768宽度QA。
- 导入页顶部：标题、上传区、3列基础设置/换行主模型、2列模式卡均在横向可视区，无横向溢出；左栏只有图标但保留可访问名称。下部尚待查。
- UI-003补充：独立visualTab850013840实测768×900；滚动检查导入页全长，候选卡2列、辅助说明、6格式复选框、入队按钮、长文件名进度卡均换行且不遮挡，无横向溢出。此宽度导入页视觉检查通过（文件选择逻辑F-006不因此关闭）。

### UI-004 — 768队列空状态
- 队列标题、添加课堂、暂停/历史按钮及空提示完整显示，无按钮重叠或水平裁切；正文与页脚间距正常。这里只覆盖空队列布局，有任务卡仍待同尺寸验证。

### UI-005 — 768设置页
- 设置/诊断双栏改为单栏；token输入与应用按钮、保留字段、开关、保存按钮均完整。诊断区刷新/下载按钮可见，长JSON在区域内显示，无页面级横向溢出（client=scroll753）。
- 仅验证当前诊断数据的布局，不推定超长错误消息亦通过。

### UI-006 — 768 IBus页与滚动状态
- IBus标题/不可用状态、预热/释放按钮、3张只读状态卡、Portal降级说明均在宽度内，纵向滚动可访问全部内容。
- 从已下滚的设置页切换IBus后scrollY仍144.5，标题在视口上方；手动上滚才见顶部。记录F-021（低）：主导航切页未归位滚动位置，可能让用户错过页面状态。需跨其他页面复测其范围。

### UI-007 — 768词典页
- 列表/详情改为上下布局，256字名称占较多高度但可完整换行；原材料词典的新增字段、导入/删除按钮、5条词条及逐行删除按钮均可见。
- 中日英读音和来源/权重列在此样例未重叠或被裁切；client=scroll753，无页面级横向溢出。较小手机宽度仍需查。

## 诊断与响应式交接
- API-ST-001/002诊断包内容与控制脱敏通过，F-020遗漏IBus状态。首次界面与接口诊断环境不同，未把硬件详情差异记为缺陷。
- 768×900现已实测有效，UI-003至007覆盖导入全长、队列空态、设置、IBus、词典；F-021切页滚动位置保留。
- 已reset视口并关闭临时visualTab850013840；保留主tab850013371在导入页，仍等用户手动选择有效WAV结果。后续可重新设置并实测视口再测其他页面，不能继续声称工具永久不可用。
- session12202运行，无模型；目标仍未完成。

### UI-008 — 768历史列表
- 实测768×900，client=scroll753；稿件卡改为单列，文件名/时间/语言/状态完整。第一页30条可滚动，末页1条及分页按钮正常，翻至短末页scrollY归0。
- 原auto_mixed语言仍未本地化（TR-001已记录），与中文标签相比可见实现值泄漏，但无横向排版溢出。

### UI-009 — 768转录详情
- 波形、播放控制、3段轨道和文本层/句段列表均在宽度内，编辑侧栏移至正文下方，client=scroll753。
- 滚动后说话人保存、textarea、撤销/重做/重跑、候选和来源/审计展开控件可达且无重叠；未执行重跑。

### UI-010 — 768模型页
- 模型卡单列，SHA长字段换行、语言标签和底部操作按钮均可见，截图检查MOSS-TD与FireRedLLM展开限制无重叠。
- 对20个实际article读取DOM scrollWidth/clientWidth，未发现卡内横向溢出；页面client=scroll753。限制展开仍含英语原文，记录既有本地化不足，非布局阻断。

### UI-011 — 768导出页与结果卡
- 三列选项、整行生成按钮、单列选择卡、跨页保留提示及结果卡均完整，文件名/67bytes/SHA/下载按钮不遮挡，client=scroll753。
- 从已滚动模型页切到导出页时scrollY=1379，需Ctrl+Home回顶部，复现并扩大F-021范围。
- 为检查结果区再次生成31号MD，成功1份；此项不重复宣称浏览器下载落地通过。

### UI-012 — 390导入页顶部与截图限制
- 初始真实390×844，client=scroll375，截图标题/描述/上传区与单列语言词典控件完整，可纵向访问。
- 滚动后DOM尺寸仍390×844，但工具截图异常缩小为不匹配的宽幅图；指定clip捕获超时。故只将顶部截图算视觉证据，下半部暂只有无水平溢出的DOM证据，不虚报完整390页通过。

## 窄屏第二轮交接
- UI-008至011补齐768历史/转录/模型/导出布局，F-021模型→导出scrollY1379复现。
- 390导入只完成顶部视觉，滚动截图异常；已reset并关闭visualTab850013844，原主tab继续等待用户手动文件选择结果。
- 无新增源码修改、无模型调用，session12202保持服务。仍不可标全量完成。

### UI-013 — 390关键页面补测
- 真实390×844视口（innerWidth390、client/scrollWidth375）下检查模型、历史稿件、转录详情、导出、设置、IBus、队列和词典；所有页面均无页面级横向滚动，主按钮未越出视口。
- 模型卡长名称/SHA换行；历史稿件单列；转录媒体按钮换行、编辑器在句段下方；导出选项改为单列；设置token输入与应用按钮并排；IBus状态卡、失败任务按钮均可访问。
- 词典256字名称占据较长纵向空间，但详情表单转单列、词条表转两列分组，删除按钮可见。本轮确认390下没有控件遮挡；F-014长名称造成过度纵向占用仍成立。
- 浏览器截图API在部分滚动操作中报告的`scrollY`与画面位置不一致，因此只以截图可见内容和横向尺寸作证，不据此判断精确纵向滚动值。

### UI-014 / F-032 — 390侧栏导航可发现性
- 390宽度下8个导航按钮的文字子元素计算样式均为`display:none`，视觉上只剩↑、◌、≡、↓、Aa、◇、●、⚙。
- 按钮均有正确`aria-label`，键盘/辅助技术名称保留；但全部`title=null`，没有浏览器原生悬停提示。对不熟悉符号的鼠标用户，队列、模型、IBus等入口不易辨认。

### GAP-001 / F-033 — 本地数据删除功能没有产品入口
- 用户手册第9节承诺`derived_only`、单任务`job`与带精确短语确认的`all_local_data`三层受控删除。
- GitNexus只定位到内部`DeletionService`和集成测试；前端无相关控件，API routes也无删除数据/任务入口。设置页“数据生命周期”只有保留天数与IBus音频开关。
- 因而普通用户无法执行手册所述删除，也无法从稿件列表删除任务；这是功能入口缺失，不是待点击控件。内部DeletionService的48项安全集成组已通过，但不能替代可用产品入口。

### GAP-002 / F-034 — 课程材料类型入口不完整
- 用户手册第5节承诺TXT、Markdown、CSV、PDF、PPTX、讲义或教材；WebUI按钮和file accept仅`.txt,.md,.csv`，也没有讲义/教材来源类型选择。
- 后端`import_material`实际包含PDF/PPTX提取，API header接受完整`TermSource`，说明能力存在但WebUI不可达。此项与F-004的建议确认/alias编辑缺失共同影响完整词典工作流。

### UP-005 / F-035 — 浏览器导入进度与服务器状态脱节
- 隔离后端启动时无对应旧任务，首页仍长期显示此前浏览器留下的2个“已入队·100%”文件；服务重启、刷新与跨日测试后均存在，且“停止未完成的导入”禁用。
- GitNexus定位`batchImports`，Serena确认所有item（包括done）写入`localStorage`，恢复时done永久保留；没有与jobs/recordings核对，也没有清除已完成记录的按钮。
- 用户更换/重置后端、删除任务或长期使用时会看到无法清除的过期进度，可能误以为任务仍属于当前服务。该问题与F-006的文件选择失败独立。

## 当前自主测试收尾状态
- 原隔离手工服务在干净启动测试后恢复为session29332，实际请求仍返回200。
- 进程检查未发现classscribe worker、dictationd、FFmpeg或bwrap；本轮从未启动真实模型。
- 隔离任务状态：28 completed、2 cancelled、1 failed，pending/running=0。人工失败/取消状态刻意保留以便复查，不会自动执行。
- 31号导出目录权限已恢复0700；SQLite事务锁已释放；设置实际保留`retention.derived_days=30`、`ibus.save_audio=false`。
- VAD隔离副本仍存在，仅因删除需明确确认而按用户指示跳过；文件已恢复正确哈希，真实用户模型目录从未写入。
- `git diff --check`、QA夹具Ruff检查通过。工作树只有`.playwright-cli/`和`docs/ManualTest/`未跟踪测试证据，未修改产品源码。
- 可自主、无需额外确认且不运行模型的测试已执行完毕。剩余项目均在“自主范围关闭状态”中标明跳过原因；因此本阶段报告完成，但原始全量目标不能标记为complete。

### ENV-008 / MO-004 — 本机既有模型只读核查及隔离副本
- 用户真实cache有5个active模型（Qwen1.7/0.6、VAD/LID、MOSS-TD），另FireRedAED无active；先前无模型只指隔离根，桌面安装入口缺失结论不受影响。
- 对4.65MB FireRedVAD的4个文件执行只读哈希校验，匹配audit aggregate；复制到隔离cache，并仅新增隔离installation记录，health_status=unknown。没有改真实cache、读API-token内容、安装依赖或加载模型。
- 脚本copy_vad_for_file_qa.py与evidence/vad-file-copy.json保留操作证据；真实权重不放入仓库。
- UI正确显示运行环境需修复，校验按钮仅文件校验/环境resolve，不推理；实际返回明确worker环境缺失及修复建议。

### F-022 — 模型状态同时显示不可用与健康通过（中）
- 同一卡片安装状态“运行环境需修复”，却同时宣称“安装完成，健康检查已通过，模型可用”。本轮记录unknown且worker缺失。
- 点击校验后明确失败，成功可用文案仍保留。容易误导用户将不可运行模型视为就绪。未将副本假定为完成健康验证。

### MO-005 — 文件损坏、拒绝回滚及恢复
- 只对隔离副本Stream-VAD/cmvn.ark追加测试字节，校验UI准确报hash or size mismatch并指出相对文件，损坏版本回滚API409。
- 已从备份恢复原字节，SHA c87f6f13edf0f0ec7535ddfc9cc3387d9268cb234b70182d566c5e2edf3ca473；再次校验只剩worker环境缺失，证明文件损坏恢复。
- 恢复后再次点回滚到当前完整revision，继续核对返回与状态；未运行模型、未动真实缓存。
- MO-005恢复后rollback返回200，未有成功提示，旧校验错误仍显示。该回滚仅激活同一完整版本，未启动任何worker。

## 已安装文件管理交接
- 隔离环境现有FireRedVAD文件副本，真实源目录未改；此前“隔离无模型文件”状态已变化，但所需ASR/结构模型仍缺，不应启动任务。
- 新modelTab停在FireRedVAD卡，文件已恢复，环境仍缺。下一步删除按钮需在浏览器永久删除确认规则下明确获准，范围仅/tmp/classscribe-manual-phase1/cache/classscribe/models/firered_vad及其隔离DB记录。
- F-022矛盾可用状态已记录；其余模型repair会安装依赖且包含健康路径，未点击。主tab仍导入等待人工交叉验证。

### MO-006 — 健康WAV参数边界
- 新Qwen0.6准备表单，依次实际选择stereo/8kHz/8bit，均出现16kHz mono16bit要求；无PCM数据报非空/帧对齐要求；240001samples（超过15秒1样本）报不能超过15秒。
- 每个文件与预期对应alert经可见性等待核对，未点击安装确认。页面同时有旧VAD校验alert，定位初次歧义后按消息限定，不误报测试失败。
- 测试文件在fixtures/health-boundaries，均人工生成，无用户录音。

### MO-007 — 健康WAV容器损坏提示
- 6个实际文件逐个上传：截断chunk header、截断chunk、重复fmt、重复data、缺fmt、非帧对齐PCM，均被对应中文alert拒绝，未到健康推理。
- 以上覆盖文件完整性前置校验的主要用户可见错误分支；不外推为所有二进制模糊测试已完成。

### MO-008 — 15秒上界与计划切换
- 恰240000samples/15秒上传成功并自动选中，健康格式错误清除；仅保留此前独立VAD环境校验错误。
- 从Qwen0.6切换ForcedAligner准备计划，许可证链接目标、下载量/所需空间与有效期更新，15秒录音/日语选择保留。没有沿用旧模型确认令牌进行提交，也未点击最终确认。

### F-023 — 安装确认区缺少明确模型身份（中）
- 准备多个模型后顶部确认区仅显示“安装确认”、大小、环境、许可证、录音和最终按钮；没有模型名称、model ID、完整revision或manifest SHA的直接可见标识。
- 当前目标只能间接从许可证链接URL辨别，Qwen模型环境信息又相同。与手册描述的可核对完整commit/manifest披露不符，增加确认错误目标风险。
- 后端绑定token是否正确不由此否定；本项是用户可见身份核对缺口。

## 健康边界测试交接
- MO-006/007共11种无效WAV拒绝、MO-008恰15秒成功，均真实文件选择，没有模型推理。
- modelTab最后是ForcedAligner准备计划，已选15秒录音，最终确认未点击；随后退出准备表单。FireRedVAD隔离副本删除仍待用户明确批准，自动goal消息不算批准。
- 主tab仍等待人工导入文件选择结果。其余样例数据/权限/锁无新增故障。

### QU-003 / JO-002 — 人工运行和失败状态
- 仅DB设置人工28号running/transcription、29号failed/audio_import，未启动执行器；所有操作仍在隔离库。
- 队列分别显示正在转录、暂停/取消按钮，以及失败、中文错误原因与重试按钮。
- 失败详情显示MEDIA_PROBE_FAILED+人工中文原因、已停止实时监听，重试失败点启用，无正文时稿件/导出不可用。未点击重试（可能进入执行器）。
- 人工28设置progress.45但UI0%来自无checkpoint的汇总，按页面说明计算，不能据此误报真实进度bug。

### JO-003 — 运行状态暂停与终态清理
- 队列点击28号暂停，此卡立即变已暂停，按钮变恢复到队尾；详情当前操作任务已暂停、SSE正常。
- 随后详情取消28号，DB已cancelled；29号仍failed。核查隔离库pending/running=0，未遗留可被恢复执行的人工运行状态。
- 仅验证状态控制与UI同步，不宣称真实推理checkpoint安全暂停已验证；该部分受不运行模型约束。

## 阶段覆盖审计与阻塞（2026-09-13）

本目标**未完成**。前述缺陷被发现不代表依赖它们的操作链已经测完；实际推理为用户明确排除项。

| 剩余要求 | 当前证据与阻塞 | 所需下一步 |
| --- | --- | --- |
| 主导入文件列表、排序/删除、批次提交/停止/重试 | 自动化选有效WAV列表为空（F-006）；健康WAV路径及真实上传接口已通过，不能替代该UI链 | 用户手动选文件交叉验证；若同样失败，需要修复入口后继续该链 |
| 模型删除UI | 已建立并校验4.65MB隔离VAD副本，真实源目录只读；删除确认已请求但未获回答 | 用户明确批准删除该隔离副本，再执行并核查DB/文件 |
| 桌面菜单、IBus输入源、全局快捷键非推理交互 | 无安装desktop/IBus/systemd条目；当前原生UI控制禁用 | 提供可操作的隔离桌面安装/人工配合验证 |
| 浏览器下载实际落地 | 下载API200与服务器产物已验证；下载事件超时，下载管理页被工具安全策略拒绝 | 用户确认实际下载文件或提供获准的下载验证路径；不绕过已拒绝页面 |
| 390窗口完整视觉 | 顶部真实尺寸可见，下滚截图缩放异常；768页布局已有实测证据 | 可靠截图/人工窄窗口验证剩余区域 |
| 涉及实际推理的运行/恢复、安装健康最终确认 | 用户明确禁止启动模型，未执行 | 保持排除，不通过伪造模型输出声称真实推理通过 |

阻塞计数：隔离副本删除确认与导入手动交叉验证在请求回合、MO-006至008回合、QU-003/JO-002至003回合连续存在；自动goal续跑不构成用户批准。独立可执行检查继续完成到本节，当前剩余关键范围需要用户输入/外部环境变化，转为blocked而非complete，避免反复重述和无效轮询。

### 恢复所需现场
- 主导入tab850013371；另modelTab用于任务/模型检查（通过浏览器tabs列表找当前ID）。
- session12202测试服务；先poll确认存活再决定恢复。隔离31份人工job中28/30已cancelled，29failed，其余completed，无pending/running。
- 原用户模型/录音未修改，隔离VAD副本已恢复正确文件；无模型加载，临时目录权限/数据库锁均已恢复。

## 2026-09-13 自动化补充检查

### AUTO-FE-001 — 前端静态检查与单元测试
- `npm --prefix frontend test -- --run`：18个测试文件、57项测试全部通过，包括上传、批量导入、稿件列表、任务详情、转录编辑、模型、词典、导出、设置和IBus组件。
- `npm --prefix frontend run lint` 与 `npm --prefix frontend run typecheck` 均以状态码0结束。
- 这些结果是补充回归证据；现有组件测试中的模拟响应不能覆盖本文件已实测的F-003、F-006、F-009等真实服务/浏览器问题。

### AUTO-FE-002 — 前端构建、格式与端到端套件
- `npm --prefix frontend run format:check` 与 `npm --prefix frontend run build` 均通过；生产构建98个模块，JS 366.19 kB（gzip 115.58 kB）。
- Playwright端到端5项中4项通过：1440/390稿件改进、桌面/移动任务活动刷新、IBus诊断降级。
- 完整工作台模拟流程失败在首个断言：用例仍查找旧标题“导入一堂课”，当前页面为“批量导入课堂”；同文件后续仍查找旧按钮“开始自动转录”，当前为“加入转录队列”。记录F-024（中）：关键端到端用例随产品改版失效，无法作为发布门有效证据。
- 首次运行因沙箱禁止4173端口失败；经批准启动本地Vite后得到上述真实结果。没有连接外网或运行模型。

### AUTO-BE-001 — 后端测试分组首轮
- 单元测试以`-x`运行：18项通过后，`test_repository_structure_and_dependency_boundaries`失败，指出`backend/classscribe/consensus/punctuation.py`使用被禁止的long-text sequence matching。记录F-025（高）：架构发布约束测试失败。
- `tests/integration/test_stage3_packaging.py`：3项全部通过。
- `tests/integration/test_api_v1.py`前11项逐项显示通过；进入名称含`runs_health_inference`的模拟安装测试后进程被外部终止143。为遵守本轮不启动模型的边界，后续跳过含健康推理语义的用例，并按明确安全模块继续；不能把被终止部分算通过或失败。

### AUTO-BE-002 — 后端安全模块与沙箱挂起复核
- 12个不含worker/真实模型的集成文件共48项全部通过；Stage3 packaging另3项通过。
- 排除架构与benchmark已知失败后，单元测试连续62项通过，随后停在`test_classroom_queue.py::test_fifo_waits_for_cleanup_and_continues_after_failure`；单测单独运行仍被外部终止143。
- `test_classroom_pipeline.py`单文件前2项通过，第三项`test_dictation_arming_waits_for_safe_boundary_and_defers_new_jobs`单独运行20秒仍未完成，timeout返回124。
- 初次暂记的F-026经沙箱外单进程复核后撤销：`test_classroom_pipeline.py`与`test_classroom_queue.py`共15项在1.34秒内全部通过。此前挂起由测试沙箱的本地IPC/调度限制触发，不是当前项目缺陷。
- 排除架构、benchmark与模型生命周期独立模块后，剩余422项单元测试在沙箱外9.75秒全部通过；同一安全测试`test_local_api_requires_bearer_token_for_all_api_routes`单独复测也通过。
- 模拟模型生命周期`test_model_inference.py`共10项通过；没有真实权重或大模型进程。当前单元测试总结果为457项通过、2项失败。
- 两项稳定失败为：F-025架构约束；F-027 `regressions.v1.json`仍引用已不存在的`test_infinite_anata_loop_is_rejected_then_split_with_full_coverage_and_model_switch`。

### AUTO-BE-003 — 全量非真实模型集成测试
- `pytest tests/integration -m 'not real_model' --ignore=test_stage3_packaging.py`：131项通过、2项真实模型测试被排除。
- 含packaging时其余133项通过、2项排除，RPM生命周期在`rpm --initdb`创建`/tmp/.../rpmdb/.rpm.lock`时权限拒绝；单独复测仍失败。此前同文件曾3项通过，当前结果按最新重复证据记为环境不稳定/权限阻塞，不认定产品生命周期通过或失败。
- 所有执行均显式排除`real_model`标记，未加载本地权重。

### AUTO-REL-001 — 发布校验与架构门不一致
- `.venv/bin/python scripts/validate_release.py`与`uv run --offline --no-sync ...`均返回0、`ready_with_waivers`；原始source_license/phase12/desktop_matrix为false，由已有精确waiver变为effective true。
- README原命令`uv run --offline`在独立空缓存因缺`hatchling>=1.27,<2`而无法构建；`--no-sync`使用现有环境可运行。记录为离线首次运行依赖缓存要求，未联网修复。
- `scripts/check_architecture.py`独立返回1：`long-text sequence matching is forbidden in consensus: backend/classscribe/consensus/punctuation.py`，与单元架构失败一致。
- GitNexus定位、Serena核对发现发布校验的`_architecture_errors`只验证`final-architecture.v1.json`和14个证据文件存在，不调用同仓库的完整架构检查。因此`final_architecture:true`未覆盖当前失败规则，记录F-028；`ready_with_waivers`不能作为本工作树全部发布门通过的证据。
- `desktop-file-validate packaging/desktop/classscribe.desktop`状态码0。

### AUTO-STATIC-001 — Python静态与语法检查
- 本轮新增5个QA夹具脚本先经Ruff格式化和自动修正；随后`ruff check .`全仓通过。初次51项均来自未格式化的QA脚本，修正后不作为产品缺陷。
- `mypy`按`pyproject.toml`规范范围检查235个源文件，无问题；`compileall`检查backend/protocol/ibus/scripts/tools状态码0。
- 将`tools/model-manifest`额外强行纳入主mypy环境会因该独立工具依赖`httpx`、`huggingface_hub`、jsonschema stubs缺失产生28项环境错误；该目录不在规范mypy `files`范围，未把它误记为产品类型失败。
- `.venv/bin/pyright`不存在，`npx --no-install pyright --version`也未返回并被中断；Pyright配置已按项目规则指向`.venv`，但本机缺可执行检查工具，记为未运行。

### AUTO-E2E-001 — 干净XDG核心启动
- 首次运行误连仍占用8765的手工测试服务，导致新进程token目录未创建；停止旧服务后，在允许本地端口的环境重跑`tests/e2e/test_stage3_clean_xdg_start.py`通过（1项，0.99秒）。
- 测试验证干净XDG下固定loopback启动、健康响应与token创建；没有安装或加载模型。完成后原隔离手工服务已恢复为session29332，库中无pending/running任务。

### AUTO-SCRIPT-001 — IBus矩阵与回归音频脚本
- `validate_ibus_desktop.py --output /tmp/classscribe-phase1-ibus-desktop.json`试图`chmod /tmp 0700`并被拒绝。Serena确认`_atomic_json`无条件执行`path.parent.chmod(0o700)`；若以高权限运行可能破坏共享目录权限，记录F-031。
- 改用专用0700目录后脚本正常返回2和`status: incomplete`，符合当前四种桌面会话均未执行的原始证据；没有伪造passed。
- `materialize_regression_audio.py`初次误传历史回归清单，明确拒绝缺format/fixtures；使用正确`tests/audio_fixtures/regression-audio.v1.json`后两次各生成3个WAV，两次SHA完全一致，确定性通过。
- `run_benchmark.py`和Phase12最终签核需要真实模型/gold/桌面证据，按用户限制与当前缺失条件跳过。

### ST-005 — API token错误与恢复
- 在独立浏览器标签应用测试用错误token，服务日志连续显示settings/diagnostics等请求403，但设置页无alert，仍显示之前缓存的数据，用户无法知道token无效；记录F-029。
- 刷新会优先采用首页注入的正确token并恢复。另一次不刷新测试中，重新输入隔离服务token`manual-test-local-only`后数据访问恢复、设置控件启用。
- 仅使用专门测试token，没有读取或传输用户真实API token；测试标签最终恢复正确token。

### NAV-001 / F-030 — 品牌返回与刷新恢复目标
- 从失败任务空稿点击品牌按钮可立即返回“批量导入课堂”，品牌导航本身通过。
- 但浏览过任务后，导航到模型或设置再刷新，都会跳回旧任务详情；品牌返回首页后再刷新也同样回旧任务。
- Serena确认`setCurrentJob`写入`sessionStorage.classscribe-current-job`，初始化时只要该值存在就固定进入job；普通`setPage`和品牌按钮不清除它。当前URL始终为`/`，用户无法从地址判断/恢复当前页。
- 失败任务的“查看稿件”虽无正文仍可点，打开后只显示“该筛选下没有句段”；与导出页会禁用空正文稿形成不一致，但主要影响已归入TR-005措辞/空态问题。
